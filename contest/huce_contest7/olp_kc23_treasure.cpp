#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
const int N = 1e3;

int main() {
    int n, S;
    cin >> n >> S;
    S *= 2;
    bitset<N+1> b[N+1];

    for (int i = 0; i < n; i++) {
        int x, y; cin >> x >> y;
        b[x][y] = 1;
    }

    for (int x = 0; x <= 1)

    return 0;

}